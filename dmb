#!/bin/bash
# Open favorite bookmarks in dmenu

bookmarks_file=~/bookmarks
keyword=$(cat $bookmarks_file | awk '{print $1}' | dmenu -fn 'Droid Sans Mono-12')
selected_link=$(awk '/'"$keyword"'/{print $2}' $bookmarks_file)

xdg-open $selected_link
